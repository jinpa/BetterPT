# MedBridge Go – copy to .env and fill in
MB_USER=your_email_or_username
MB_PASS=your_password
# Optional: one access code from your PT (for export_workout.py)
# MB_TOKEN=your_access_code
# Optional: label for this program in exported JSON (e.g. "knee", "elbow")
# MB_TOKEN_NAME=knee
