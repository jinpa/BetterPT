# MedBridge Go – copy to .env and fill in
MB_USER=your_email_or_username
MB_PASS=your_password

# Multiple programs in one run (export_workout.py writes workout_<name>.json for each):
# MB_TOKENS=knee:ACCESS_CODE_1,elbow:ACCESS_CODE_2

# Or single program (optional):
# MB_TOKEN=your_access_code
# MB_TOKEN_NAME=knee
